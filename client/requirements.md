## Packages
recharts | For visualizing expense data by category
framer-motion | For smooth page transitions and interactive animations
date-fns | For beautiful date formatting

## Notes
The application communicates with a backend that processes WhatsApp webhooks.
The simulator page sends requests to POST /api/webhook.
The dashboard fetches data from GET /api/expenses.
